<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
                             http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>com.sysmlvisualizer</groupId>
  <artifactId>sysml-parse-cli</artifactId>
  <version>1.0.0-SNAPSHOT</version>
  <packaging>jar</packaging>

  <properties>
    <maven.compiler.release>21</maven.compiler.release>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <!--
      Version of the locally built Pilot Implementation fat JAR.
      Install it once into your local Maven repository before building:

        mvn install:install-file \
          -Dfile=$HOME/official-backends/SysML-v2-Pilot-Implementation/\
      org.omg.sysml.interactive/target/org.omg.sysml.interactive-0.59.0-SNAPSHOT-all.jar \
          -DgroupId=org.omg.sysml \
          -DartifactId=org.omg.sysml.interactive \
          -Dversion=0.59.0-SNAPSHOT \
          -Dclassifier=all \
          -Dpackaging=jar \
          -DgeneratePom=true
    -->
    <pilot.version>0.59.0-SNAPSHOT</pilot.version>
  </properties>

  <dependencies>
    <!--
      The SysML v2 Pilot Implementation fat JAR.
      Contains: SysMLStandaloneSetup, KerMLStandaloneSetup, XtextResourceSet,
                EMF Resource/EPackage APIs, and the full Xtext + EMF runtime.
    -->
    <dependency>
      <groupId>org.omg.sysml</groupId>
      <artifactId>org.omg.sysml.interactive</artifactId>
      <version>${pilot.version}</version>
      <classifier>all</classifier>
    </dependency>
  </dependencies>

  <build>
    <plugins>

      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>3.13.0</version>
        <configuration>
          <release>21</release>
        </configuration>
      </plugin>

      <!--
        Produce target/sysml-parse-cli.jar — a self-contained fat JAR.

        ServicesResourceTransformer merges META-INF/services/* entries so that
        Xtext SPI registrations (parsers, validators, serialisers) survive
        being repacked from multiple source JARs into one output JAR.
      -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-shade-plugin</artifactId>
        <version>3.6.0</version>
        <executions>
          <execution>
            <phase>package</phase>
            <goals><goal>shade</goal></goals>
            <configuration>
              <finalName>sysml-parse-cli</finalName>
              <createDependencyReducedPom>false</createDependencyReducedPom>
              <transformers>
                <transformer implementation="org.apache.maven.plugins.shade.resource.ManifestResourceTransformer">
                  <mainClass>com.sysmlvisualizer.parser.SysmlParseCli</mainClass>
                </transformer>
                <transformer implementation="org.apache.maven.plugins.shade.resource.ServicesResourceTransformer"/>
              </transformers>
            </configuration>
          </execution>
        </executions>
      </plugin>

    </plugins>
  </build>

</project>
